{
  "name": "qios-workers",
  "version": "0.1.0",
  "description": "QiOS Workers - Shared dependencies for all Cloudflare Workers",
  "private": true,
  "scripts": {
    "deploy:orchestrator": "wrangler deploy orchestrator",
    "deploy:memory": "wrangler deploy memory",
    "deploy:embedder": "wrangler deploy embedder",
    "deploy:ingestion": "wrangler deploy ingestion",
    "deploy:self-heal": "wrangler deploy self_heal",
    "deploy:metadata-naming": "wrangler deploy metadata_naming",
    "deploy:semantic-router": "wrangler deploy semantic_router"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.84.0",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.50.0"
  }
}

