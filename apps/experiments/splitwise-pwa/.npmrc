# Disable strict peer dependencies for Vercel builds
strict-peer-dependencies=false

# Workaround: pnpm will show warnings about ignored scripts
# but Prisma's explicit `prisma generate` in build scripts will still run

