# Public Assets

Place your logo file here as `logo.svg`.

The logo will automatically appear in the header when present.

Supported formats:
- SVG (recommended)
- PNG
- JPG/JPEG

File should be named `logo.svg` (or update the path in `src/App.jsx`).

