
[project]
name = "qid-cli"
version = "0.1.0"
description = "Interactive CLI for issuing and stamping root/child QIDs into Markdown files"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "QiAlly" }]

[tool.setuptools]
py-modules = ["qid"]
