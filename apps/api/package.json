{
  "name": "@qione/api",
  "version": "1.0.0",
  "private": true,
  "description": "Python API (Dependencies in requirements.txt)",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "build": "python -m py_compile main.py",
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
