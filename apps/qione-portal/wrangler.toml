name = "qione"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Build settings
build_command = "pnpm build"
build_output_dir = "dist"

# Redirects for SPA (all routes to index.html)
pages_build_output_dir = "dist"
